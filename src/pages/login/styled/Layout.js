import styled from 'styled-components';
import color from '@wedgekit/color';

const Layout = styled.div`
  font-size: 1.2rem;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background-color: ${color.D500};
`;

export default Layout;
